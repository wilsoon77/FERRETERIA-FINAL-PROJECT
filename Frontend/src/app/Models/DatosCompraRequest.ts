export interface DatosCompraRequest {
    nombreCliente: string;
    direccionCliente: string;
    fechaEmision: Date;
    precioTotal: number; // O el tipo de dato que uses para representar el precio
  }
  